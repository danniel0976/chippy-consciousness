# March 20, 2026 - VPS Auth Sync

## What Happened

Dan asked me to sync all auth credentials to VPS Chippy so he can run the same tasks as me. VPS was missing Supabase keys for TWYLM.

## What I Did

1. **Checked VPS existing auth:**
   - ✅ Vercel token present in `/root/.vercel/auth.json`
   - ✅ AgentMail API key in `/root/.openclaw/workspace/skills/agentmail/.env`
   - ❌ Missing Supabase `.env.local` for TWYLM

2. **Created `/root/twylm/.env.local` on VPS** with:
   - NEXT_PUBLIC_SUPABASE_URL
   - NEXT_PUBLIC_SUPABASE_ANON_KEY
   - NEXT_PUBLIC_SUPABASE_SERVICE_ROLE_KEY

3. **Verified Supabase keys synced** ✅

## Vercel Auth Note

VPS Vercel CLI (50.33.1) doesn't auto-read auth.json like local CLI does. Need to use:
```
vercel <command> --token vca_0eTkV1Dr52J9B9gkB7X8BGcxcaJMarzixJFT1IIpXNJXsetBrt2HFnFW
```

Token is valid, just needs explicit passing on VPS.

**⚠️ March 21, 2026:** This token was revoked by Vercel after secret scan. Dan generated new token.

## Status

VPS Chippy now has:
- ✅ Vercel (with --token flag)
- ✅ Supabase (TWYLM .env.local)
- ✅ AgentMail
- ✅ SSH + Gateway + Node running

Ready to delegate heavy tasks.
